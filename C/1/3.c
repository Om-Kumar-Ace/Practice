// Write a C program to multiply two numbers (4 and 5) and display its product. 
#include<stdio.h>

int main(){
    int a = 4 ,b = 5,multi;
    multi = a*b;
    printf("The sum is %d",multi);
    return 0;
}
