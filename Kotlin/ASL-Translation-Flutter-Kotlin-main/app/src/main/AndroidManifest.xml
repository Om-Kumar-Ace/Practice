<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools">


    <dist:module dist:instant="true" />

    <!-- features -->
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.microphone" android:required="true"/>
    <!-- permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- This permission is not required for API level 29 and above -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!--Might be needed for external storage so added requestLegacyExternal Storage-->
    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:taskAffinity=""
        android:theme="@style/Theme.GestureRecognizer"
        tools:ignore="AllowBackup">

        <!-- Declare the SplashScreenActivity -->
        <activity android:name=".SplashScreenActivity"
            android:exported="true"
            android:theme="@style/SplashTheme"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <!-- Set SplashScreenActivity as the launcher activity -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- Adding FlutterHostActivity for flutter -->
        <activity
            android:name=".FlutterHostActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:configChanges="orientation|screenSize">
          <!--  removed intent-filter from here to splashscreen -->
        </activity>




        <activity
            android:name=".MainActivity"
            android:exported="false">
            <!-- removed intent-filter from here-->

        </activity>

        <meta-data
            android:name="android.notch_support"
            android:value="true" />
    </application>

</manifest>
